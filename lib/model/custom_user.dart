class CustomUser {
  //Esto no es más que un objeto al cual llenaremos de atributos
  String uid;
  String? email;
  String? userName;

  CustomUser({
    required this.uid,
    this.email,
    this.userName,
  });
}

